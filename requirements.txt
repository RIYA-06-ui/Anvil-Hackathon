# Persistent Context Engine — Minimal Dependencies
# 
# Core engine uses Python stdlib only (per requirements).
# These are for testing, benchmarking, and development only.

# Core (used in existing project)
networkx==3.3
numpy==1.26.4
orjson==3.10.3

# Testing (used in your test suite)
pytest>=7.4.3
pytest-cov>=4.1.0

# Code quality
black==24.4.2
flake8==7.0.0
